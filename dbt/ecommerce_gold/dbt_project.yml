name: 'ecommerce_gold'
version: '1.0.0'
profile: 'ecommerce_gold'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  ecommerce_gold:
    staging:
      +materialized: view        # staging = lightweight views
    marts:
      +materialized: table       # marts = persisted Gold tables
